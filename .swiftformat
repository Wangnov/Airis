# SwiftFormat 配置 - Airis 项目
# 适用于 Swift CLI 工具项目

# ------------------------------------------------------------------------------
# Swift 版本和基础配置
# ------------------------------------------------------------------------------
--swiftversion 5.10

# 排除目录
--exclude **/.build,**/DerivedData,**/.swiftpm

# ------------------------------------------------------------------------------
# 缩进和行宽
# ------------------------------------------------------------------------------
--indent 4
--maxwidth 120

# 参数换行策略
--wraparguments before-first
--wrapcollections before-first

# ------------------------------------------------------------------------------
# Import 排序
# ------------------------------------------------------------------------------
--importgrouping testable-bottom

# ------------------------------------------------------------------------------
# 空格和格式化
# ------------------------------------------------------------------------------
--linebreaks lf
--allman false
--self remove
--semicolons inline
--trimwhitespace always

# ------------------------------------------------------------------------------
# 移除文件头部注释
# ------------------------------------------------------------------------------
--header strip

# ------------------------------------------------------------------------------
# 启用的规则（默认启用的不需要重复声明）
# ------------------------------------------------------------------------------
# 以下规则默认启用，这里列出以明确项目标准：
# - sortedImports (import 排序)
# - hoistPatternLet (提升 let/var 到模式外)
# - void (使用 Void 而非 ())
# - trailingCommas (多行集合添加尾随逗号)
# - spaceAroundOperators (运算符周围空格)
# - consecutiveBlankLines (限制连续空行)
# - blankLinesBetweenScopes (声明之间添加空行)
# - blankLinesAtStartOfScope (移除作用域开头空行)
# - blankLinesAtEndOfScope (移除作用域结尾空行)
# - redundantReturn (移除单表达式闭包的 return)
# - redundantBackticks (移除不必要的反引号)
# - redundantBreak (移除冗余 break)

# ------------------------------------------------------------------------------
# 可选启用的规则
# ------------------------------------------------------------------------------
--enable isEmpty

# ------------------------------------------------------------------------------
# 禁用的规则（根据 CLI 项目特性）
# ------------------------------------------------------------------------------
# 无需禁用特定规则，使用默认配置
